name: Parse Repository

on:
  workflow_dispatch:
    inputs:
      repo:
        description: 'Source repo (org/name)'
        required: true
      tag:
        description: 'Git tag to parse'
        required: true
      storage_repo:
        description: 'Storage repo (repolex-forx/org--name)'
        required: true

jobs:
  parse:
    runs-on: ubuntu-latest
    steps:
      - name: Setup storage repo
        env:
          GH_TOKEN: ${{ secrets.REPOLEX_PAT }}
        run: |
          if ! gh repo view ${{ inputs.storage_repo }} &>/dev/null; then
            gh repo create ${{ inputs.storage_repo }} \
              --public \
              --description "RDF parse data for ${{ inputs.repo }}" \
              --homepage "https://github.com/${{ inputs.repo }}"
          fi

          git clone "https://x-access-token:${GH_TOKEN}@github.com/${{ inputs.storage_repo }}.git" storage || {
            mkdir storage && cd storage && git init
            git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${{ inputs.storage_repo }}.git"
          }

          cd storage
          git config user.name "repolex-bot"
          git config user.email "bot@repolex.ai"

          if ! git rev-parse HEAD &>/dev/null; then
            echo "Initializing" > README.md
            git add README.md
            git commit -m "Initial commit"
            git branch -M main
            git push -u origin main
          fi

      - name: Clone and checkout source
        run: |
          git clone https://github.com/${{ inputs.repo }}.git /tmp/source
          cd /tmp/source
          git fetch --tags
          git checkout ${{ inputs.tag }}
          echo "COMMIT_SHA=$(git rev-parse HEAD)" >> $GITHUB_ENV

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install repolex
        env:
          GH_TOKEN: ${{ secrets.REPOLEX_PAT }}
        run: |
          uv tool install "git+https://x-access-token:${GH_TOKEN}@github.com/repolex-ai/repolex-parser-py.git"
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Cache LSP servers
        id: lsp-cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.local/share/uv/tools/repolex/lib/python3.12/site-packages/multilspy/language_servers
            ~/.local/share/uv/tools/repolex/lib/python3.13/site-packages/multilspy/language_servers
            ~/.multilspy
          key: lsp-servers-${{ runner.os }}-v3
          restore-keys: |
            lsp-servers-${{ runner.os }}-

      - name: Warm LSP servers
        if: steps.lsp-cache.outputs.cache-hit != 'true'
        run: |
          echo "Cache miss - warming all LSP servers..."
          PYTHON_VER=$(python3 -c "import sys; print(f'{sys.version_info.major}.{sys.version_info.minor}')")
          LSP_DIR="$HOME/.local/share/uv/tools/repolex/lib/python${PYTHON_VER}/site-packages/multilspy/language_servers"

          echo "=== LSP server directory: $LSP_DIR ==="
          ls -la "$LSP_DIR/" 2>/dev/null || echo "Directory not found"

          # Warm each language server by creating a minimal LanguageServer instance
          # This triggers the download of runtime dependencies
          python3 << 'PYEOF'
          import os, sys, time, tempfile
          from pathlib import Path

          # Add the uv tool path
          python_ver = f"{sys.version_info.major}.{sys.version_info.minor}"
          site_packages = Path.home() / f".local/share/uv/tools/repolex/lib/python{python_ver}/site-packages"
          sys.path.insert(0, str(site_packages))

          from multilspy.multilspy_config import MultilspyConfig, Language
          from multilspy.multilspy_logger import MultilspyLogger
          from multilspy.language_server import LanguageServer

          languages = [
              ("Python", Language.PYTHON),
              ("Java", Language.JAVA),
              ("TypeScript", Language.TYPESCRIPT),
              ("JavaScript", Language.JAVASCRIPT),
              ("Rust", Language.RUST),
              ("Go", Language.GO),
              ("Ruby", Language.RUBY),
              ("C#", Language.CSHARP),
              ("Kotlin", Language.KOTLIN),
              ("Dart", Language.DART),
              ("C++", Language.CPP),
          ]

          logger = MultilspyLogger()

          for name, lang in languages:
              start = time.time()
              print(f"\n{'='*60}")
              print(f"Warming {name} LSP server...")
              print(f"{'='*60}")
              try:
                  with tempfile.TemporaryDirectory() as tmpdir:
                      # Create a dummy file so the server has something to work with
                      dummy = Path(tmpdir) / "dummy.txt"
                      dummy.write_text("// dummy\n")

                      config = MultilspyConfig.from_dict({"code_language": lang})
                      lsp = LanguageServer.create(config, logger, str(tmpdir))
                      elapsed = time.time() - start
                      print(f"  OK - {name} ready ({elapsed:.1f}s)")
              except Exception as e:
                  elapsed = time.time() - start
                  print(f"  WARN - {name} failed ({elapsed:.1f}s): {e}")

          print(f"\n{'='*60}")
          print("LSP warm-up complete!")
          print(f"{'='*60}")
          PYEOF

      - name: Parse repository
        env:
          REPOLEX_STORAGE_DIR: ${{ github.workspace }}/storage
          GH_TOKEN: ${{ secrets.REPOLEX_PAT }}
        run: |
          cd /tmp/source
          repolex parse ${{ inputs.repo }} ${{ inputs.tag }}

      - name: Commit parse results
        env:
          GH_TOKEN: ${{ secrets.REPOLEX_PAT }}
        run: |
          cd ${{ github.workspace }}/storage
          git add .
          git diff --staged --quiet && echo "No changes to commit" && exit 0
          git pull --rebase origin main || true
          git commit -m "Parse ${{ inputs.repo }}@${{ inputs.tag }} (${{ env.COMMIT_SHA }})"
          git push

      - name: Aggregate repository
        env:
          REPOLEX_STORAGE_DIR: ${{ github.workspace }}/storage
          GH_TOKEN: ${{ secrets.REPOLEX_PAT }}
        run: |
          cd /tmp/source
          repolex aggregate ${{ inputs.repo }} ${{ inputs.tag }}

      - name: Commit aggregate results
        env:
          GH_TOKEN: ${{ secrets.REPOLEX_PAT }}
        run: |
          cd ${{ github.workspace }}/storage
          git add .
          git diff --staged --quiet && echo "No aggregate changes to commit" && exit 0
          git pull --rebase origin main || true
          git commit -m "Aggregate ${{ inputs.repo }}@${{ inputs.tag }} (${{ env.COMMIT_SHA }})"
          git push

      - name: Generate README
        run: |
          cd ${{ github.workspace }}/storage
          PARSE_DATE=$(date -u +"%Y-%m-%d")

          TREE=$(find . -name '*.nq.gz' -o -name '*.nq' | grep -v '.git' | sed 's|^\./||' | sort | head -200 | tree --fromfile -n 2>/dev/null || find . \( -name '*.nq.gz' -o -name '*.nq' \) -not -path './.git/*' | sed 's|^\./||' | sort | awk -F/ '
          {
            for (i=1; i<NF; i++) {
              prefix = ""
              for (j=1; j<i; j++) prefix = prefix "    "
              dir = prefix $i "/"
              if (!(dir in seen)) { print dir; seen[dir]=1 }
            }
            prefix = ""
            for (j=1; j<NF; j++) prefix = prefix "    "
            print prefix $NF
          }' | head -60)

          cat > README.md << 'READMEEOF'
          # Repolex Knowledge Graph of ${{ inputs.repo }}

          RDF knowledge graph data for [${{ inputs.repo }}](https://github.com/${{ inputs.repo }}), parsed by [repolex](https://repolex.ai).

          > **Note**: This data is experimental and subject to change without notice.

          ## How to use this data

          The easiest way to get started is to install the [lexq](https://github.com/repolex-ai/lexq) query tool using [uv](https://docs.astral.sh/uv/getting-started/installation/).

          If you have uv installed, just copy/paste this into your terminal:

          ```bash
          uv tool install git+https://github.com/repolex-ai/lexq
          ```

          This installs lexq onto your system, in your user context. Verify the install:

          ```bash
          lexq --help
          ```

          **lexq is designed to be used primarily by LLMs in a terminal.** Start up your favorite LLM and ask it to use the lexq tool. It's that easy!

          To load this repo's data:

          ```bash
          lexq download ${{ inputs.repo }}
          ```

          This will automatically download essential data files from the last parsed commit. Consult `lexq --moreinfo` for other options, including downloading multiple commits, blobs, etc.

          ## Data structure

          All data is stored as gzip-compressed [N-Quads](https://www.w3.org/TR/n-quads/) (`.nq.gz`), a standard RDF format that can be loaded into any triplestore or graph database.

          ```
          READMEEOF

          sed -i 's/^          //' README.md

          echo "$TREE" >> README.md
          echo '```' >> README.md

          cat >> README.md << 'DESCEOF'

          | Directory | What it contains |
          |-----------|-----------------|
          | `blob/` | Per-file AST graphs, content-addressed by git blob SHA. Each file in the source repo gets its own graph. |
          | `aggregate/ast/` | Combined AST graph per parsed commit. Merges all blob graphs for a snapshot of the entire codebase at that point. |
          | `aggregate/lsp/` | Language Server Protocol enrichment: resolved symbols, definitions, references, and type information. |
          | `aggregate/dataflow/` | Interprocedural data flow edges between functions and modules. |
          | `aggregate/repolex/` | Combined graph (AST + LSP + dataflow) per commit. |
          | `commit/` | Git commit metadata (author, date, message, parent links). |
          | `branch/` | Branch metadata. |
          | `tag/` | Tag metadata. |
          | `filetree/` | File tree snapshots per commit (which files existed and their blob SHAs). |

          ## Source repository

          [${{ inputs.repo }}](https://github.com/${{ inputs.repo }})

          ---
          *Parsed on PARSE_DATE_PLACEHOLDER by [repolex](https://repolex.ai)*
          DESCEOF

          sed -i "s/PARSE_DATE_PLACEHOLDER/${PARSE_DATE}/" README.md

      - name: Commit README
        env:
          GH_TOKEN: ${{ secrets.REPOLEX_PAT }}
        run: |
          cd ${{ github.workspace }}/storage
          git add README.md
          git diff --staged --quiet && echo "No README changes" && exit 0
          git pull --rebase origin main || true
          git commit -m "Update README for ${{ inputs.repo }}@${{ inputs.tag }}"
          git push
